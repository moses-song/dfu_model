_BASE_: ../coco/instance-segmentation/swin/maskformer2_swin_base_IN21k_384_bs16_50ep.yaml
MODEL:
  SEM_SEG_HEAD:
    NUM_CLASSES: 1
DATASETS:
  TRAIN: ("wound_train",)
  TEST: ("wound_val",)
DATALOADER:
  NUM_WORKERS: 2
SOLVER:
  IMS_PER_BATCH: 2
  BASE_LR: 0.0001
  MAX_ITER: 10000
  STEPS: (7000, 9000)
  WARMUP_ITERS: 100
TEST:
  EVAL_PERIOD: 1000
INPUT:
  IMAGE_SIZE: 512
  MIN_SCALE: 1.0
  MAX_SCALE: 1.0
